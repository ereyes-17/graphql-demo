mutation {
    addEmployer(name: "SpaceX", contactEmail: "hr@spacex.com") {
        employer {
            id
            name
            contactEmail
            industry
        }
        authenticatedAs
    }
}